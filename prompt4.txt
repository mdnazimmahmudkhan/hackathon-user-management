Refactor UserServiceImpl.registerUsermethod. Make the validation with an IValidator Interface and its implementations for each validation logic. We can reuse these validators later for updating the user.